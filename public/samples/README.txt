Drop the three demo document images here, with these exact filenames:

  maria-snap.png       — the SNAP food-assistance notice
  aisha-bill.png       — the Metro Health ER bill
  david-discharge.png  — the Riverside discharge instructions

The landing-page scenario buttons load these. If a file is missing, the button
falls back to the document's text automatically, so it always works.
(.jpg also fine — just update the `image` path in src/lib/data.ts to match.)
